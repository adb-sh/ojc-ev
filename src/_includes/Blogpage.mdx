---
layout: Raw.tsx
title: Blog
---

<h1 class="my-8 font-thin text-5xl">News</h1>

<div>
  {page.data.results.map((page) => (
    <a href={page.url}>
      <comp.BlogListItem page={page} />
    </a>
    // <div>
    //   {JSON.stringify(Object.keys(page))}
    // </div>
  ))}
</div>

<nav>
  <ul class="pagination join">
    {page.data.pagination.previous ? (
      <li class="page-item">
        <a
          class="page-link btn btn-secondary join-item"
          href={`/news/${page.data.pagination.previous}`}
        >
          &laquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link btn btn-secondary join-item btn-disabled">&laquo;</a>
      </li>
    )}
    {new Array(page.data.pagination.totalPages).fill().map((_, index) => (
      <li class="page-item">
        <a class="page btn btn-secondary join-item" href={`/news/page/${index + 1}`}>
          {index + 1}
        </a>
      </li>
    ))}
    {page.data.pagination.next ? (
      <li class="page-item">
        <a
          class="page-link btn btn-secondary join-item"
          href={`/news/${page.data.pagination.next}`}
        >
          &raquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link btn btn-secondary join-item btn-disabled">&raquo;</a>
      </li>
    )}
  </ul>
</nav>

<a class="btn btn-primary my-2" href="/feed.rss">
  RSS-Feed
</a>
